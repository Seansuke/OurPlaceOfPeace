// Pressing Right Key
switch(subMenu) {
    case "Party":
        with(menus[menuPos]) {
            event_user(2);
        }
    break;
    case "Settings":
        with(menus[menuPos]) {
            event_user(2);
        }
    break;
}
