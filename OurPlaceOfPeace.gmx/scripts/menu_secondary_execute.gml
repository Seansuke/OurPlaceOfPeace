switch(subMenu)
{
    case "Party":
        with(menus[menuPos]) {
            event_user(0);
        }
    break;
    case "Settings":
        with(menus[menuPos]) {
            event_user(0);
        }
    break;
}
